import type { Metadata } from "next";
import Link from "next/link";
import { ChevronLeft } from "lucide-react";
import { AdminPageHeader } from "@/components/admin/admin-page-header";
import { createProperty } from "../actions";

export const metadata: Metadata = { title: "Add Property" };
const inputClass = "mt-2 min-h-11 w-full border border-black/15 bg-white px-3.5 text-sm outline-none focus:border-[#8f713d]";

export default function NewPropertyPage() {
  return <main className="mx-auto max-w-4xl px-4 py-7 sm:px-6 lg:px-8 lg:py-9"><Link href="/admin/properties" className="mb-5 inline-flex min-h-10 items-center gap-2 text-xs font-semibold text-black/45 hover:text-black"><ChevronLeft aria-hidden="true" className="size-4" />All properties</Link><AdminPageHeader eyebrow="Portfolio onboarding" title="Add Property" description="Create the core property identity first. Systems, members, vendors, and documents can be linked from the property record afterward." /><form action={createProperty} className="mt-6 space-y-5 border border-black/10 bg-[#f8f6f0] p-5 sm:p-7"><section><h2 className="font-serif text-2xl">Property identity</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold text-black/55">Property name<input name="name" required maxLength={160} className={inputClass} /></label><label className="text-xs font-semibold text-black/55">Property type<input name="property_type" required maxLength={120} placeholder="Single-family residence" className={inputClass} /></label><label className="text-xs font-semibold text-black/55 sm:col-span-2">Address<input name="address_line_1" required maxLength={240} className={inputClass} /></label><label className="text-xs font-semibold text-black/55 sm:col-span-2">Address line 2<input name="address_line_2" maxLength={240} className={inputClass} /></label><label className="text-xs font-semibold text-black/55">City<input name="city" required defaultValue="Park City" maxLength={120} className={inputClass} /></label><div className="grid grid-cols-2 gap-3"><label className="text-xs font-semibold text-black/55">State<input name="state" required defaultValue="UT" maxLength={2} className={inputClass} /></label><label className="text-xs font-semibold text-black/55">Postal code<input name="postal_code" maxLength={20} className={inputClass} /></label></div></div></section><section className="border-t border-black/10 pt-5"><h2 className="font-serif text-2xl">Stewardship baseline</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold text-black/55">Occupancy<select name="occupancy_type" defaultValue="second_home" className={inputClass}><option value="primary">Primary residence</option><option value="second_home">Second home</option><option value="other">Other</option></select></label><label className="text-xs font-semibold text-black/55">Property health<select name="health_status" defaultValue="Healthy" className={inputClass}><option>Healthy</option><option>Monitor</option><option>Action Recommended</option><option>Critical</option></select></label><label className="text-xs font-semibold text-black/55 sm:col-span-2">Stewardship summary<textarea name="summary" maxLength={4000} className={`${inputClass} min-h-32 resize-y py-3`} /></label></div></section><div className="flex justify-end border-t border-black/10 pt-5"><button type="submit" className="min-h-12 bg-[#171a19] px-6 text-xs font-semibold text-white">Create Property</button></div></form></main>;
}
