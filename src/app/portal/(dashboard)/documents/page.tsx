import { Download, FileText } from "lucide-react";
import { Panel, PortalPageHeader } from "@/components/portal/portal-ui";
import { documents } from "@/lib/portal-data";

export default function DocumentsPage() { return <main className="mx-auto max-w-[1480px] px-4 py-7 sm:px-7 lg:px-10 lg:py-10"><PortalPageHeader eyebrow="Property library" title="Documents" description="Plans, warranties, manuals, service records, inspection reports, and essential property references." /><Panel title="Silver Pine Residence" eyebrow="28 documents" className="mt-7"><div className="divide-y divide-black/10">{documents.map((document) => <article key={document.name} className="flex items-center gap-4 px-5 py-5 sm:px-6"><span className="flex size-11 shrink-0 items-center justify-center border border-black/10 bg-white"><FileText aria-hidden="true" className="size-4 text-[#80632d]" /></span><div className="min-w-0 flex-1"><h2 className="truncate text-sm font-medium">{document.name}</h2><p className="mt-1 text-xs text-black/40">{document.type} · Updated {document.updated}</p></div><button type="button" aria-label={`Download ${document.name}`} className="inline-flex size-10 items-center justify-center border border-black/10"><Download aria-hidden="true" className="size-4 text-black/42" /></button></article>)}</div></Panel></main>; }
